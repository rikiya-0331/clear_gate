<div class="container mx-auto px-4 py-8">
  <div class="bg-white shadow-md rounded-lg p-6 mb-6">
    <h1 class="text-2xl font-bold mb-4 text-gray-800">質問詳細</h1>

                    <div class="mb-4">
                      <p class="text-lg font-semibold text-gray-700">英語の質問文:</p>
                      <div class="flex items-center space-x-2">
                        <p class="text-xl text-gray-900"><%= @question.title_en -%></p><%= render "shared/audio_button", url: audio_question_path(@question) -%>
                      </div>
                    </div>
            
                    <div class="mb-6">
                      <p class="text-lg font-semibold text-gray-700">日本語訳:</p>
                      <p class="text-xl text-gray-900"><%= @question.title_jp %></p>
                    </div>    <div class="mt-8 flex flex-col sm:flex-row items-center justify-center gap-4">
      <%= link_to("マイページに戻る", mypage_path, class: "w-full sm:w-auto bg-primary hover:opacity-90 text-white font-bold py-2 px-4 rounded-lg transition duration-300 ease-in-out text-center") %>
      <%= link_to("質問一覧に戻る", questions_path, class: "w-full sm:w-auto bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition duration-300 ease-in-out text-center") %>
    </div>
  </div>
</div>